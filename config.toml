# WoWTranslate Proxy Configuration
http_port = 7654
workers = 4
cache_db = "translations.db"
stale_ttl = 60
scan_interval = 0.05

# Free Fallback â€” zero setup or API key needed
[[backends]]
type = "google"
timeout = 8
